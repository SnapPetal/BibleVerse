// For format details, see https://aka.ms/vscode-remote/devcontainer.json or this file's README at:
// https://github.com/microsoft/vscode-dev-containers/tree/v0.195.0/containers/java
{
  "name": "Java",
  "build": {
    "dockerfile": "Dockerfile",
    "args": {
      "INSTALL_MAVEN": "true",
      "MAVEN_VERSION": "3.8.6",
      "INSTALL_GRADLE": "false",
      "NODE_VERSION": "lts/*"
    }
  },
  // Configure tool-specific properties.
  "customizations": {
    // Configure properties specific to VS Code.
    "vscode": {
      // Set *default* container specific settings.json values on container create.
      "settings": {
        "maven.executable.path": "/usr/local/sdkman/candidates/maven/current/bin/mvn"
      },
      // Add the IDs of extensions you want installed when the container is created.
      "extensions": [
        "visualstudioexptteam.vscodeintellicode",
        "vscjava.vscode-java-pack",
        "vscjava.vscode-java-debug",
        "vscjava.vscode-java-test",
        "vscjava.vscode-java-dependency",
        "vscjava.vscode-spring-boot-dashboard",
        "vscjava.vscode-spring-initializr",
        "vscjava.vscode-maven",
        "redhat.vscode-commons",
        "redhat.java",
        "redhat.vscode-xml",
        "redhat.vscode-yaml",
        "eamodio.gitlens",
        "yzhang.markdown-all-in-one",
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "streetsidesoftware.code-spell-checker"
      ]
    }
  },
  "postCreateCommand": "mvn clean verify && chown -R node:node /home/node && aws configure import --profile personal --region us-east-1 --csv file:///home/node/.aws/tbecker_accessKeys.csv",
  "remoteUser": "vscode",
  "remoteEnv": {
    "AWS_PROFILE": "personal",
    "JAVA_HOME": "/usr/lib/jvm/msopenjdk-current"
  },
  "features": {
    "aws-cli": "latest"
  },
  "mounts": [
    "source=${localEnv:HOME}/.gitconfig,target=/home/vscode/.gitconfig,type=bind,consistency=cached",
    "source=${localEnv:HOME}/.aws,target=/home/vscode/.aws,type=bind,consistency=cached",
    "source=${localEnv:HOME}/.ssh,target=/home/vscode/.ssh,type=bind,consistency=cached"
  ]
}